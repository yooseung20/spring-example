{{>layout/header}}
    <H1>스프링 부트로 시작하는 웹 서비스</H1>
    <div class="col-md-12">
        <div class="row">
            <div class="col-md-6">
                <a href="/posts/save" role="button" class="btn btn-primary">글 등록</a>
<!--               머스테치는 다른 언어와 같은 if문을 제공하지 않음
                   true/flase만 판단한다
                   머스테치에서는 항상 최종값을 넘겨줘야 한다.
                   userName이 있다면 userName 노출
                    -->
                {{#userName}}
                    Logged in as: <span id="user">{{userName}}</span>

<!--                스프링 시큐리티에서 기본적으로 제공하는 로그아웃 URL('/logout')
                    개발자가 별도로 컨트롤러 만들 필요 없음
                    -->
                    <a href="/logout" class="btn btn-info active" roe="button">Logout</a>
                {{/userName}}

<!--            해당값이 존재하지 않는 경우 ^ 사용
                userName이 없다면 로그인 버튼을 노출
                /authorization/google
                스프링 시큐리티에서 기본적으로 제공하는 로그인 url
                별도의 컨트롤러 만들 필요 없음-->
                {{^userName}}
                   <a href="/oauth2/authorization/google" class="btn btn-success active" role="button">Google Login</a>
                    <a href="/oauth2/authorization/naver" class="btn btn-secondary active" role="button">Naver Login</a>
                {{/userName}}
            </div>
        </div>
    </div>
<!-- 목록 출력 영역 -->
<table class="table table-horizontal table-bordered">
    <thead class="thead-strong">
    <tr>
        <th>게시글번호</th>
        <th>제목</th>
        <th>작성자</th>
        <th>최종수정일</th>
    </tr>
    </thead>
    <tbody id="tbody">

<!--post 라는 list를 순회한다.-->
<!--java의 for 문과 동일하게 생각하면 된다.-->
    {{#posts}}
        <tr>
<!--list에서 뽑아낸 객체의 필드 -->
            <td>{{id}}</td>
            <td><a href="/posts/update/{{id}}">{{title}}</a></td>
            <td>{{author}}</td>
            <td>{{modifiedDate}}</td>
        </tr>
    {{/posts}}
    </tbody>
</table>
</div>
{{>layout/footer}}